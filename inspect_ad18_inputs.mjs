import {createRequire} from 'node:module'; const {chromium}=createRequire(import.meta.url)('playwright-core'); const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}); const p=await b.newPage(); await p.goto('https://www.ad18.ca/signup',{waitUntil:'domcontentloaded',timeout:15000}); console.log(await p.locator('form').evaluateAll(fs=>fs.map(f=>f.outerHTML.slice(0,5000)))); await b.close();
