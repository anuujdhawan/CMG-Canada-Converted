import {createRequire} from 'node:module'; const {chromium}=createRequire(import.meta.url)('playwright-core'); const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}); const p=await b.newPage(); await p.goto('https://www.ad18.ca/signup',{waitUntil:'domcontentloaded',timeout:15000}); const f=p.locator('form'); await f.locator('input').nth(0).fill('Commonwealth Migration Group Inc.'); await f.locator('input').nth(1).fill('commonwealthmigration'); await f.locator('input[type=email]').fill('seolaunchers@gmail.com'); await f.locator('button[type=button]').click(); await p.waitForTimeout(300); console.log('DATE BODY',(await p.locator('body').innerText()).slice(-1800)); console.log('BUTTONS',await p.locator('button').allTextContents()); await b.close();
