import {createRequire} from 'node:module'; const {chromium}=createRequire(import.meta.url)('playwright-core'); const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}); const p=await b.newPage(); await p.goto('https://business.google.com/?gmbsrc=in-en-z-z-z-gmb-s-z-u~bgcmhp-sso-u&hl=en&skipLandingPage=true&skipPagesList=1&cbl=sso&cbp=bgc&cbt=sso',{waitUntil:'domcontentloaded',timeout:25000}); await p.waitForTimeout(2000); console.log('URL',p.url(),'TITLE',await p.title()); console.log('BODY',(await p.locator('body').innerText()).slice(0,2400)); console.log('BUTTONS',await p.locator('button,a').evaluateAll(es=>es.map(e=>({tag:e.tagName,text:(e.innerText||'').trim().slice(0,100),href:e.href||''})).filter(x=>/add|create|start|business|profile|continue/i.test(x.text+' '+x.href)).slice(0,60))); await b.close();
