import {createRequire} from 'node:module'; const {chromium}=createRequire(import.meta.url)('playwright-core'); const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}); const p=await b.newPage(); await p.goto('https://phreemarketplace.com/sign-up?redirect=%2Fclassifieds%3Fpost%3Dtrue',{waitUntil:'domcontentloaded',timeout:20000}); await p.locator('#email').fill('info@commonwealthmigration.ca'); await p.locator('#password').fill('Cmg-Phree-2026!Migrate'); await p.locator('#confirm').fill('Cmg-Phree-2026!Migrate'); await p.locator('#age').check(); await p.locator('#terms').check(); await p.getByRole('button',{name:'Sign Up'}).click(); await p.waitForLoadState('domcontentloaded').catch(()=>{}); await p.waitForTimeout(1200); console.log('url',p.url()); console.log((await p.locator('body').innerText()).slice(0,5000)); await b.close();
