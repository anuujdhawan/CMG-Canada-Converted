import {createRequire} from 'node:module'; const {chromium}=createRequire(import.meta.url)('playwright-core');
const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}); const p=await b.newPage(); await p.goto('https://www.businessbacklinks.co.uk/addbusiness/online.php',{waitUntil:'domcontentloaded',timeout:20000}); await p.locator('input[name="category"]').fill('legal-services'); await p.getByRole('button',{name:'Continue'}).click(); await p.waitForLoadState('domcontentloaded').catch(()=>{}); await p.waitForTimeout(500); console.log('URL',p.url(),'TITLE',await p.title()); console.log((await p.locator('body').innerText()).slice(0,5000)); console.log('FIELDS',await p.locator('input,textarea,select,button').evaluateAll(es=>es.map(e=>({tag:e.tagName,type:e.type||'',name:e.name||'',id:e.id||'',text:(e.innerText||e.value||'').trim().slice(0,80)})))); await b.close();
