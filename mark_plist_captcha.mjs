import {FileBlob,SpreadsheetFile} from '@oai/artifact-tool';
const path='outputs/2026-09-20-directory-prospects/commonwealth-migration-free-directory-prospects.xlsx'; const wb=await SpreadsheetFile.importXlsx(await FileBlob.load(path)); const sh=wb.worksheets.getItem('Directory Prospects'); sh.getRange('K14').values=[['Manual CAPTCHA required']]; sh.getRange('O14').values=[['Free signup flow reached step 2; CAPTCHA is required before account creation and profile submission. Playwright did not bypass it.']]; sh.getRange('P14').values=[['https://plist.ca/signup_2']]; const out=await SpreadsheetFile.exportXlsx(wb); await out.save(path); console.log('updated');
