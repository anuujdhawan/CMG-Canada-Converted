import {createRequire} from 'node:module'; const {chromium}=createRequire(import.meta.url)('playwright-core'); const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}); const p=await b.newPage(); await p.goto('https://plist.ca/signup',{waitUntil:'domcontentloaded',timeout:20000}); await p.waitForTimeout(1000); console.log(await p.locator('input,select,textarea,button').evaluateAll(es=>es.map(e=>({tag:e.tagName,type:e.type||'',name:e.name||'',id:e.id||'',text:(e.innerText||e.value||'').trim().slice(0,100)})))); console.log(await p.locator('form').evaluateAll(fs=>fs.map(f=>({action:f.action,method:f.method})))); await b.close();
