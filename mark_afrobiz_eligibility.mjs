import {FileBlob,SpreadsheetFile} from '@oai/artifact-tool';
const path='outputs/2026-09-20-directory-prospects/commonwealth-migration-free-directory-prospects.xlsx'; const wb=await SpreadsheetFile.importXlsx(await FileBlob.load(path)); const sh=wb.worksheets.getItem('Directory Prospects');
sh.getRange('K50').values=[['Eligibility confirmation required']]; sh.getRange('O50').values=[['Playwright confirmed the official Peel route states “It is free to be listed” and exposes a full listing form. The directory is specifically for Black-owned businesses; no submission was made because that eligibility fact was not verified from the supplied business details.']]; sh.getRange('P50').values=[['https://www.afrobiz.ca/add-my-Black-Owned-business-canada']]; const out=await SpreadsheetFile.exportXlsx(wb); await out.save(path); console.log('updated');
