import {createRequire} from 'node:module'; const {chromium}=createRequire(import.meta.url)('playwright-core'); const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}); const p=await b.newPage(); await p.goto('https://solutions.yp.ca/free-listing',{waitUntil:'domcontentloaded',timeout:25000}); await p.waitForTimeout(1800); console.log('URL',p.url(),'TITLE',await p.title()); console.log('BODY',(await p.locator('body').innerText()).slice(0,2500)); console.log('FIELDS',await p.locator('input,textarea,select,button').evaluateAll(es=>es.map(e=>({tag:e.tagName,type:e.type||'',name:e.name||'',id:e.id||'',text:(e.innerText||e.value||'').trim().slice(0,80)})).slice(0,80))); await b.close();
