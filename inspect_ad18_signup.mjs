import {createRequire} from 'node:module'; const {chromium}=createRequire(import.meta.url)('playwright-core'); const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}); const p=await b.newPage(); try{await p.goto('https://www.ad18.ca/signup',{waitUntil:'domcontentloaded',timeout:20000});await p.waitForTimeout(700);console.log('URL',p.url(),'TITLE',await p.title());console.log('FIELDS',await p.locator('input,textarea,select,button').evaluateAll(es=>es.map(e=>({tag:e.tagName,type:e.type||'',name:e.name||'',id:e.id||'',text:(e.innerText||e.value||'').trim().slice(0,70)})).slice(0,100)));console.log('BODY',(await p.locator('body').innerText()).slice(0,2200));}catch(e){console.log('ERROR',e.message)}await b.close();
