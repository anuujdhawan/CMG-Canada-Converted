import {createRequire} from 'node:module'; const {chromium}=createRequire(import.meta.url)('playwright-core'); const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}); const p=await b.newPage(); await p.goto('https://plist.ca/signup'); await p.waitForTimeout(500); console.log(await p.locator('#skill_cat option').evaluateAll(es=>es.map(e=>({t:e.textContent,v:e.value})))); await b.close();
