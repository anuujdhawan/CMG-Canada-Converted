import {createRequire} from 'node:module'; const {chromium}=createRequire(import.meta.url)('playwright-core'); const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}); const p=await b.newPage(); await p.goto('https://canadabusinessdirectory.net/submit-listing/',{waitUntil:'domcontentloaded',timeout:25000}); await p.waitForTimeout(1500); console.log('URL',p.url(),'TITLE',await p.title()); console.log('BODY',(await p.locator('body').innerText()).slice(0,3000)); console.log('FIELDS',await p.locator('input,select,textarea,button').evaluateAll(es=>es.map(e=>({tag:e.tagName,type:e.type||'',name:e.name||'',id:e.id||'',text:(e.innerText||e.value||'').trim().slice(0,100)})).slice(0,100))); console.log('FORMS',await p.locator('form').evaluateAll(fs=>fs.map(f=>({action:f.action,method:f.method})).slice(0,10))); await b.close();
